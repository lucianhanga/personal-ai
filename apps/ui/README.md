# apps/ui (`@personalai/ui`)

The PersonalAI **desktop/web UI** — a Tauri shell wrapping a web SPA.

- The SPA framework (React vs Svelte) is decided in **M0-6** and recorded as an ADR.
- Talks to the loopback backend only (authenticated).
- Tests: Vitest (unit/component) + **Playwright** (e2e) are set up in M0-6.

> Placeholder package at M0-1; real scaffolding arrives in M0-6.
