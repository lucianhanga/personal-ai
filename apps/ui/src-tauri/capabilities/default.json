{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Minimal, capability-based permission set for the main window (deny-by-default).",
  "windows": ["main"],
  "permissions": ["core:default"]
}
