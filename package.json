{
  "name": "personal-ai",
  "version": "0.0.0",
  "private": true,
  "description": "Local-first, omni-capable, structured-output-first, security-first AI assistant.",
  "license": "Apache-2.0",
  "packageManager": "pnpm@11.5.1",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck",
    "test": "pnpm -r --if-present test",
    "build": "pnpm -r --if-present build"
  }
}
