{
  "name": "@personalai/contracts",
  "version": "0.0.0",
  "private": true,
  "description": "PersonalAI TS/Zod bindings for the structured-output contracts (aligned with the Python schemas and the canonical JSON Schema).",
  "license": "Apache-2.0",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "test": "vitest run",
    "lint": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0",
    "vite": "^6.0.0",
    "vitest": "^4.1.0"
  }
}
