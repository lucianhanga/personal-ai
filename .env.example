# Example environment file. Copy to .env and fill in. NEVER commit real secrets.
# Remote providers are OFF by default; only set keys for providers you explicitly enable.

# PERSONALAI_BIND=127.0.0.1
# PERSONALAI_EGRESS=off
# OLLAMA_HOST=http://127.0.0.1:11434
